# Defects

* overriding defaulted attribute doesn't work
    * base type with attribute that has default value
    * extend type
    * override attribute with default value
    * attribute value is the base default value instead of the extended value